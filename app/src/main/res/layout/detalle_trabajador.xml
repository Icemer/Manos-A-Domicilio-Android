<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/stitchBackground">

    <!-- ===== HEADER VERDE (banner superior) ===== -->
    <View
        android:id="@+id/headerBg"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@color/stitchPrimary"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Título centrado sobre el header -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Perfil del Profesional"
        android:textColor="@color/white"
        android:textSize="17sp"
        android:textStyle="bold"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Ícono favorito (top-right) -->
    <ImageView
        android:id="@+id/ivFavorito"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_favorite_border"
        android:tint="@color/white"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="16dp"
        android:clickable="true"
        android:focusable="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ===== AVATAR (circular, sobre el banner) ===== -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardAvatar"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="140dp"
        app:cardCornerRadius="60dp"
        app:cardElevation="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/ivDetalleAvatar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/avatar_perfil"
            android:scaleType="centerCrop" />
    </androidx.cardview.widget.CardView>

    <!-- ===== NOMBRE ===== -->
    <TextView
        android:id="@+id/tvDetalleNombre"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nombre del Trabajador"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/stitchOnSurface"
        android:layout_marginTop="12dp"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintTop_toBottomOf="@id/cardAvatar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ===== RATING ===== -->
    <LinearLayout
        android:id="@+id/llRating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="6dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="4dp"
        android:background="@color/stitchSurfaceContainerLow"
        app:layout_constraintTop_toBottomOf="@id/tvDetalleNombre"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@android:drawable/btn_star_big_on"
            app:tint="@color/stitchStar" />

        <TextView
            android:id="@+id/tvDetalleRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0.0"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/stitchOnSurface"
            android:layout_marginStart="4dp" />
    </LinearLayout>

    <!-- ===== CATEGORÍA ===== -->
    <TextView
        android:id="@+id/tvDetalleCategoria"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Categoría"
        android:textSize="14sp"
        android:textColor="@color/stitchOnSurfaceVariant"
        android:layout_marginTop="6dp"
        app:layout_constraintTop_toBottomOf="@id/llRating"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ===== REGISTRO ===== -->
    <TextView
        android:id="@+id/tvDetalleRegistro"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Registrado desde: --"
        android:textSize="12sp"
        android:textColor="@color/stitchOnSurfaceVariant"
        android:layout_marginTop="3dp"
        app:layout_constraintTop_toBottomOf="@id/tvDetalleCategoria"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ===== ESTADO DISPONIBILIDAD ===== -->
    <TextView
        android:id="@+id/tvDetalleEstado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Disponible"
        android:textSize="13sp"
        android:textColor="@color/stitchSecondary"
        android:textStyle="bold"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/tvDetalleRegistro"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ===== TARJETA DESCRIPCIÓN ===== -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardDescripcion"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/stitchOutlineVariant"
        app:strokeWidth="1dp"
        app:cardBackgroundColor="@color/stitchSurfaceContainerLowest"
        app:layout_constraintTop_toBottomOf="@id/tvDetalleEstado"
        app:layout_constraintBottom_toTopOf="@id/btnSolicitarServicio"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Descripción"
                android:textColor="@color/stitchOnSurface"
                android:textSize="15sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tvDetalleDescripcion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Aquí va la descripción detallada del trabajador..."
                android:textSize="14sp"
                android:textColor="@color/stitchOnSurfaceVariant"
                android:lineSpacingExtra="4dp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- ===== BOTÓN SOLICITAR SERVICIO ===== -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSolicitarServicio"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:text="Solicitar Servicio"
        android:textSize="15sp"
        android:textStyle="bold"
        android:backgroundTint="@color/stitchPrimary"
        android:textColor="@color/white"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="8dp"
        app:cornerRadius="12dp"
        app:layout_constraintBottom_toTopOf="@id/menuInferior"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ===== BOTTOM MENU ===== -->
    <include
        layout="@layout/bottom_menu"
        android:id="@+id/menuInferior"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
